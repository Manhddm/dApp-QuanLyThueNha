interface ProfileModalProps {
    isOpen: boolean;
    onClose: () => void;
    requireFullInfo?: boolean;
}
export default function ProfileModal({ isOpen, onClose, requireFullInfo }: ProfileModalProps): import("react/jsx-runtime").JSX.Element;
export {};
